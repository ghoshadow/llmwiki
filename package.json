{
  "name": "llmwiki",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "shared",
    "mcp-server",
    "web-ui"
  ],
  "scripts": {
    "build": "npm run build -w shared && npm run build -w mcp-server && npm run build -w web-ui",
    "dev": "npm run build -w shared && npm run build -w mcp-server && npm start -w mcp-server & npm run dev -w web-ui",
    "start": "npm run start -w mcp-server"
  },
  "dependencies": {
    "@radix-ui/react-scroll-area": "^1.2.10",
    "@radix-ui/react-slot": "^1.2.4",
    "@radix-ui/react-tabs": "^1.1.13",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.6.0"
  }
}
